# SPDX-License-Identifier: MIT
# SPDX-FileCopyrightText: "2025 smdn <smdn@smdn.jp>"
#
# ref: https://docs.github.com/code-security/dependabot/working-with-dependabot/dependabot-options-reference
#
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "wednesday"
      time: "03:19"
      timezone: "Asia/Tokyo"
    assignees:
      - smdn
    groups:
      GitHub-Actions-Workflows:
        patterns:
          - "actions/*"
    ignore:
      # actions/checkout: report major updates only
      - dependency-name: "actions/checkout"
        update-types:
          - "version-update:semver-minor"
          - "version-update:semver-patch"
      # actions/setup-python: report major and minor updates only
      - dependency-name: "github/codeql-action/*"
        update-types:
          - "version-update:semver-patch"
